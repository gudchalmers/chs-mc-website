version: '3'

services:
  chs-mc-website:
    build: .
    ports:
      - "3000:3000"
    environment:
      PORT: 3000
      DB_USER: chs-mc-website
      DB_PASS: 5a7XoNwyRKgDyr9sA2rh
      DB_NAME: mc_stats
      DB_HOST: mariadb
      MAIL_FROM: hello@example.com
      MAIL_NAME: 'CHS Minecraft Server'
      MAIL_HOST: sandbox.smtp.mailtrap.io
      MAIL_PORT: 2525
      MAIL_USER: ed34e3c7508a9d
      MAIL_PASS: e278e352efca4b
      MAIL_SSL: false
  mariadb:
    image: mariadb:11
    container_name: mariadb
    volumes:
      - mariadb:/var/lib/mysql
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: 5a7XoNwyRKgDyr9sA2rh
      MYSQL_DATABASE: mc_stats
      MYSQL_USER: chs-mc-website
      MYSQL_PASSWORD: 5a7XoNwyRKgDyr9sA2rh

volumes:
  mariadb:
